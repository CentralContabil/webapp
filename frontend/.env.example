# Opcional: URL absoluta da API. Se vazio, em dev o Vite faz proxy de /api → VITE_API_PROXY_TARGET.
# VITE_API_URL=http://192.168.0.47:8000

# Alvo do proxy (só dev). API deve estar escutando aqui.
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
