{
  "name": "webapp-desktop-root",
  "private": true,
  "description": "Atalho: o monorepo (NFe, SPED, SPED merge, Consolidado SCI) fica em webapp-01. npm run dev na raiz delega para webapp-01.",
  "scripts": {
    "dev": "npm run dev --prefix webapp-01",
    "dev:stack": "npm run dev:stack --prefix webapp-01",
    "redis:up": "npm run redis:up --prefix webapp-01",
    "install:app": "npm install --prefix webapp-01",
    "build": "npm run build --prefix webapp-01",
    "test:sci-py": "npm run test:sci-py --prefix webapp-01"
  }
}
